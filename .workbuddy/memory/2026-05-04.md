# 2026-05-04 Daily Log

## UniAPI 服务器升级 v3.1.0 → v3.8.1

- GitHub 最新 Release: v3.0.0 (2026-04-27)，GitHub 最新 tag: v3.8.1
- 服务器原版本: v3.1.0 (5948ccf-dirty)，手动编译的 dev 版本
- 备份位置: `/opt/uniapi-backup-20260504/`（含旧二进制、数据库、docker-compose.yml、service 文件）
- 旧二进制保留: `/opt/uniapi/uniapi.v3.1.0`（63M）
- 本地 CGO_ENABLED=0 交叉编译失败（go-sqlite3 需要 CGO）
- 解决方案: 在服务器上安装 Go 1.25 + gcc，直接编译（CGO_ENABLED=1）
- 服务器源码位置: `/opt/uniapi-source/`
- 升级后版本: v3.8.1 (adf079f)，API 验证通过
- 回退方法: `systemctl stop uniapi && cd /opt/uniapi && mv uniapi uniapi.v3.8.1 && mv uniapi.v3.1.0 uniapi && systemctl start uniapi`

## 前端缺失修复

- 首次编译 `go build` 时 `web/build/` 为空，导致前端 HTML 未嵌入（`GET /` 返回 0 字节）
- 修复: 从本地上传 `web/build/modern/` 到服务器 `/opt/uniapi-source/web/build/`，重新编译
- 注意: `//go:embed web/build/*` 要求该目录下不能有空目录（如 `web/build/build/` 会报错）
- 整理完整升级部署 SOP 到 Obsidian（`服务器运维/UniAPI 服务器升级部署 SOP.md`），含一键脚本和 5 个踩坑记录
