# 2026-05-05 工作记忆

## SOP Review & 修订
- 对 UniAPI 升级部署 SOP 进行全面 review，发现 8 个问题
- P0 修复 3 个：方案 B 命令错误、缺少清空旧 build、旧二进制命名含空格
- P1 修复 4 个：增加磁盘检查、外部验证、空目录清理、失败回退逻辑
- P2 改善 1 个：环境要求区分必需/可选
- SOP 已写入 Obsidian，Review 报告同步输出

## UniAPI 升级 v3.8.1 → v3.8.3（最终成功）
- Step 0：检查通过（磁盘 102G 可用，最新 tag v3.8.3）
- Step 1：备份到 `/opt/uniapi-backup-20260505/`
- Step 2：源码切换到 v3.8.3 (393c04f)
- Step 3：本地构建前端，传入 `VITE_APP_VERSION="v3.8.3"`（在本地执行 `cd web/modern && VITE_APP_VERSION="v3.8.3" npm run build`）
- 上传前先 `rm -rf /opt/uniapi-source/web/build/*`，再 scp 上传
- Step 4：编译 `CGO_ENABLED=1`，产物 63M，dynamically linked ✅
- Step 5：停服 → `mv uniapi uniapi.v3.8.1.bak` → 替换 → 重启
- Step 6：验证全部通过
  - 后端版本：3.8.3 (393c04f) ✅
  - 管理页面：HTTP 200, 1622 bytes ✅
  - 外部访问：HTTP 200, 1622 bytes ✅
  - 日志：无 FATAL/ERROR ✅
- `VITE_APP_VERSION` 已正确注入构建产物（grep 确认）

## 关键经验
- 前端 `VITE_APP_VERSION` 必须在构建时显式传入，否则 footer 显示默认值 `1.0.0`
- 构建命令：`cd web/modern && VITE_APP_VERSION="v3.8.3" npm run build`
- scp 上传前必须清空目标目录，否则旧文件残留被 `go:embed` 嵌入
